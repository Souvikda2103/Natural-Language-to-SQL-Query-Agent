# Natural-Language-to-SQL-Query-Agent
It allows users to ask questions in plain English, automatically converts them into validated SQL queries, executes them safely, and returns results in a natural-language answer.
